// importacao do express
const express = require('express');

// Inicializacao com a funcao express()
const app = express();

// Exportacao da variavel que contem a funcao do express para o arquivo server.js
module.exports = app;